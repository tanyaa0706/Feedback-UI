import { UpdateTrainingRequest } from './update-training-request';

describe('UpdateTrainingRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateTrainingRequest()).toBeTruthy();
  });
});
