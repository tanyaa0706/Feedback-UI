import { AddTrainingRequest } from './add-training-request';

describe('AddTrainingRequest', () => {
  it('should create an instance', () => {
    expect(new AddTrainingRequest()).toBeTruthy();
  });
});
